language: node_js
dist: trusty
addons:
  chrome: stable
services:
  - docker
before_install:
  - docker run -d -p 4444:4444 vvoyer/selenium-standalone
install:
  - npm ci
  - npm install -g codecov
script:
  - npm run test:ci
after_success:
  - npm run test:unit:coverage:ci