import { searchSubmit } from "."

describe("searchSubmit", () => {
  it("works", () => {
    // Arrange
    // const valA = { search: " " }
    // const valB: FormikActions<{ search: string }> = { setStatus(" GO!") }
    // Act
    const received = searchSubmit

    // Assert
    expect(received)
  })
})
